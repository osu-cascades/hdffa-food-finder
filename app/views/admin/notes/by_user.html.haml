.subnav
  .title
    %h1= @note.user

%table.table
  %thead
    %tr
      %th Notes
      %th
  %tbody
    - @note.user.notes.each do |note|
      %tr
        %td= link_to note.title, [:admin, note]
        %td
  