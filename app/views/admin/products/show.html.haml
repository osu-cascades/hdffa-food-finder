.subnav
  .title
    %h1= @product.name
  .actions
    = link_to 'Delete', [:admin, @product], method: :delete, data: { confirm: 'Are you sure?' }, class: 'danger'
    = link_to 'Edit', edit_admin_product_path(@product), class: 'primary'