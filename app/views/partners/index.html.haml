<!-- #map -->

%body{:style => "background-image: url('../../../assets/cow_background.jpg'); background-size: cover; background-width: 100%;"}
  %div{:style => "height: 500px"}
    %div{:class => "space-right"}
    %div{:style => "width: 35%; float: right;"}
      %strong{:class => "large-white-text", :style => "position: absolute; top: 255px;"}Find Partners
      %input{:class => "form-control", :style => "height: 40px; border-radius: 20px; width: 20%; position: absolute; top: 300px;"}
      //= form_with url: "/search", method: :get do |form| form.text_field :query end
      //, :style => "position: absolute; left: 810px; top: 255px;"
      //, :style => "height: 40px; border-radius: 20px; width: 300px; position: absolute; left: 800px; top: 300px;"
<!-- OLD MAP CODE
%table#partners.table.mt-5
  %thead
    %tr
      %th Name
      %th Categories
      %th Products
      %th
  %tbody
    - @partners.each do |partner|
      %tr
        %td= link_to partner.name, partner
        %td= partner.categories.name
        %td= partner.products.name
        %td
          = link_to 'Details', partner_path(partner), class: 'btn btn-sm btn-outline-primary'

:javascript
  var map = L.map('map').setView([44.0581700, -121.3153100], 13)
  map.locate({setView: true, maxZoom: 16});

  L.tileLayer('https://api.mapbox.com/styles/v1/{id}/tiles/{z}/{x}/{y}?access_token=pk.eyJ1IjoibWFwYm94IiwiYSI6ImNpejY4NXVycTA2emYycXBndHRqcmZ3N3gifQ.rJcFIG214AriISLbB6B5aw',
    {
      maxZoom: 18,
      attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors, ' +
        '<a href="https://creativecommons.org/licenses/by-sa/2.0/">CC-BY-SA</a>, ' +
        'Imagery © <a href="https://www.mapbox.com/">Mapbox</a>',
      id: 'mapbox/streets-v11',
      tileSize: 512,
      zoomOffset: -1
    }
  ).addTo(map)

  $.getJSON('/partners.json', (jsonData) => {
    jsonData.forEach( (partner) => {
      var latlon = L.latLng({lat: partner.latitude, lng: partner.longitude})
      L.marker(latlon).bindPopup(partner.name).on('mouseover', function(e) {
        this.openPopup()
      }).on('mouseout', function(e) {
        this.closePopup()
      }).addTo(map)
    })
  })

  function onLocationFound(e) {
    var radius = e.accuracy;

    L.marker(e.latlng).addTo(map)
        .bindPopup("You are within " + radius + " meters from this point").openPopup();
  }

  function onLocationError(e) {
    alert(e.message);
  }

  map.on('locationfound', onLocationFound);
  map.on('locationerror', onLocationError);
  -->
